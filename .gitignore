node_modules
dist
*.log
.DS_Store
# NB: this repo commits bun.lock (the text lockfile) — CI runs
# `bun install --frozen-lockfile`. Do NOT ignore it. (The old binary
# bun.lockb is not used.)
.claude/
.npmrc
