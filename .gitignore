node_modules
dist
*.log
.DS_Store
# NB: this repo commits bun.lock (the text lockfile)  - CI runs
# `bun install --frozen-lockfile`. Do NOT ignore it. (The old binary
# bun.lockb is not used.)
.claude/
.npmrc

# Native sim binary (built from sim.zig with your own zig; platform-specific)
packages/core/test/fixtures/wasm/sim
packages/core/test/fixtures/wasm/*.o
examples/hold-and-win/maths/sim
examples/hold-and-win/maths/*.o
