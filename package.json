{
  "name": "open-rgs-libs",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/open-rgs/libs.git"
  },
  "workspaces": [
    "packages/*",
    "games/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p packages/grid && tsc --noEmit -p packages/weights && tsc --noEmit -p packages/selectors && tsc --noEmit -p packages/fill && tsc --noEmit -p packages/markov && tsc --noEmit -p packages/recipes && tsc --noEmit -p packages/holdwin",
    "test": "bun test",
    "changeset": "changeset",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
